#include <iostream>


using namespace std;
int main(){
  /*
  int k = 2; int* kPtr = &k;                        //k is row and l is column position of max value
  int l = 8; int* lPtr = &l;                        //Adress of pointer is kPtr, and its value is *kPtr
  */
  int k = 2;
  cout << "this is k: " << k << endl;
  cout << "this is &k: " << &k << endl;
  //cout << "this is kPtr: " << kPtr << endl;
  //cout << "this is *kPtr: " << *kPtr << endl;
  return 0;
}
